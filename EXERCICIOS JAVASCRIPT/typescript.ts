/*class car {                      //orientação a objetos, algo como diferentes variaveis dentro de uma variavel , que se chama objeto
    private model:string         // A exemplo o objeto car , que tem 3 classes 
    private doorsquantity: number
    private speed:number = 0

    constructor(model:string, doorsquantity:number){
       this.model = model
       this.doorsquantity = doorsquantity
    }

    public increaseSpeed():void{                 // METODO
        this.speed = this.speed + 10
    }

    public stop(){
        this.speed = 0
    }

}


const palio = new car('palio',4)
*/


/*class Aluno {              //AQUI DEFINIMOS A NOSSA CLASS = ALUNO, QUE RECEBE ESTRUTURAS, QUE POR SUA VEZ              
    Nome:string               //RECEBERÃO OBJETOS A PARTIR DA NOSSA CLASS
    Matricula:number     
    Notas:number            
}
    
    let anderson: Aluno;         // DECLARAMOS O NOME DO OBJETO , NESSE CASO O NOME DO ALUNO "ANDERSON"
    anderson = new Aluno();      // DECLARAMOS ANDERSON COMO NOVO OBJETO RELATIVO A CLASSE "ALUNO"
    anderson.Nome = anderson;    // USAMOS . DEPOIS DO OBJETO PARA CONFIGURAR OS ATRIBUTOS QUE ELE RECEBE
    anderson.Matricula = 123;    // OS MESMOS QUE FORAM DECLARADOS NA CLASS.
    anderson.Notas = 8;
    console.log("Aluno :>> ", anderson);  // IMPRESSÃO DO OBJETO CRIADO A PARTIR DA NOSSA CLASSE

    */


